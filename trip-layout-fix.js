/* 旅伴旅行管家 · 行程详情布局修复 */
(function(){
  const esc=v=>String(v??'').replace(/[&<>\"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;'}[c]));
  const css=`.lv-trip-city-tabs{display:flex;gap:10px;overflow-x:auto;padding:4px 0 10px;scrollbar-width:none}.lv-trip-city-tabs::-webkit-scrollbar{display:none}.lv-trip-city-tab,.lv-trip-date-tab{flex:0 0 auto;border:1px solid var(--line);background:#fff;color:#555;border-radius:14px;cursor:pointer;font-weight:800}.lv-trip-city-tab{padding:11px 20px;font-size:14px}.lv-trip-city-tab.on,.lv-trip-date-tab.on{background:#6958f5;color:#fff;border-color:#6958f5;box-shadow:0 7px 18px rgba(105,88,245,.18)}.lv-trip-date-tabs{display:flex;gap:8px;overflow-x:auto;padding:2px 0 12px;scrollbar-width:none}.lv-trip-date-tabs::-webkit-scrollbar{display:none}.lv-trip-date-tab{min-width:92px;padding:9px 12px;text-align:center}.lv-trip-date-tab b{display:block;font-size:12px}.lv-trip-date-tab small{display:block;font-size:10px;margin-top:3px;opacity:.78}.lv-trip-section-label{font-size:12px;color:var(--muted);font-weight:800;margin:4px 0 7px}`;
  function style(){if(document.getElementById('lv-trip-layout-style'))return;const s=document.createElement('style');s.id='lv-trip-layout-style';s.textContent=css;document.head.appendChild(s)}
  function trip(){return window.db?.trips?.find(x=>x.id===window.activeTrip)||window.db?.trips?.[0]}
  function plan(){const t=trip();return t?.plans?.find(x=>x.id===window.activePlan)||t?.plans?.[0]}
  function hasAlternate(t){return !!(t&&(t.hasAlternateRoute===true||t.alternateRoute===true||t.useAlternateRoutes===true||t.hasBackupRoute===true||t.alternate===true))}
  function selectedCities(t){if(!t)return [];if(Array.isArray(t.cityDurations)&&t.cityDurations.length)return t.cityDurations.map(x=>x.city).filter(Boolean);return [...new Set(String(t.city||'').split(/[·,、/|]+/).map(x=>x.trim()).filter(Boolean))]}
  function dayCityMap(t,p){const days=p?.days||[];const out=[];const durations=Array.isArray(t?.cityDurations)?t.cityDurations.filter(x=>x&&x.city&&Number(x.days)>0):[];if(durations.length){let idx=0;durations.forEach(x=>{for(let k=0;k<Number(x.days)&&idx<days.length;k++,idx++)out[idx]=x.city})}days.forEach((d,i)=>{if(d&&d.city)out[i]=d.city});if(!out.filter(Boolean).length){const cs=selectedCities(t);if(cs.length)for(let i=0;i<days.length;i++)out[i]=cs[Math.min(cs.length-1,Math.floor(i*cs.length/days.length))]}return out}
  function render(){
    const box=document.getElementById('tripDetail'),t=trip();if(!box||!t)return;const plans=t.plans||[],showPlan=hasAlternate(t)&&plans.length>1;if(!showPlan)window.activePlan=plans[0]?.id||window.activePlan;const p=plan();if(!p)return;const days=p.days||[],map=dayCityMap(t,p),cities=selectedCities(t);let activeCity=window.__lvTripCity;if(!cities.includes(activeCity))activeCity=cities[0]||map[window.activeDay]||'';window.__lvTripCity=activeCity;let cityDayIndexes=days.map((d,i)=>i).filter(i=>!activeCity||map[i]===activeCity);if(!cityDayIndexes.length)cityDayIndexes=days.map((d,i)=>i);if(!cityDayIndexes.includes(window.activeDay))window.activeDay=cityDayIndexes[0]??0;const d=days[window.activeDay]||days[0];
    const cityTabs=cities.map(c=>`<button type="button" class="lv-trip-city-tab ${c===activeCity?'on':''}" data-city="${esc(c)}">${esc(c)}</button>`).join('');const dateTabs=cityDayIndexes.map(i=>{const x=days[i];return `<button type="button" class="lv-trip-date-tab ${i===window.activeDay?'on':''}" data-day="${i}"><b>${esc(x.label||('DAY '+(i+1)))}</b><small>${esc(x.date||'')}</small></button>`}).join('');
    const scheme=`<div class="scheme-head"><div><div class="crumb">${esc(t.name||'行程')}</div><div class="scheme-name">${esc(showPlan?(p.name||'方案 A'):'行程详情')}</div><div class="sub">${esc(activeCity||t.city||'')} · ${t.people||1}人</div></div><div class="actions">${showPlan?'<button class="mini" onclick="openPlanEditor()">方案管理</button>':''}<button class="mini primary" onclick="openDayEditor()">＋ 添加日程</button></div></div>`;
    const planTabs=showPlan?`<div class="tabs" style="margin-top:10px">${plans.map(x=>`<button type="button" class="tab ${x.id===window.activePlan?'on':''}" data-plan="${esc(x.id)}">${esc(x.name||('方案 '+x.id))}</button>`).join('')}</div>`:'';
    const cityBlock=cities.length?`<div class="lv-trip-section-label">选择城市</div><div class="lv-trip-city-tabs">${cityTabs}</div>`:'';const dateBlock=`<div class="lv-trip-section-label">${esc(activeCity||'行程日期')}</div><div class="lv-trip-date-tabs">${dateTabs}</div>`;const items=d?.items||[];
    const detail=`<div class="card glass"><div class="crumb">${esc(d?.date||'')}</div><h3>${esc(d?.title||'待安排')}</h3><div class="sub">每天独立编辑 · 景点 / 美食 / 酒店 / 火车 / 航班 / 交通 / 备注</div></div><div class="timeline">${items.length?items.map((i,k)=>{i._index=k;return `<div class="it-card glass"><div class="it-time">${esc(i.time||'未定时间')} <span class="it-type">${esc(i.type||'日程')}</span></div><div class="it-title">${esc(i.name||'未命名日程')}</div><div class="it-address">📍 ${esc(i.address||'暂无地址')}</div>${i.budget?`<div class="data-meta">预算 ¥${i.budget}</div>`:''}<div class="actions"><button class="mini" onclick='LvbanCopy(${JSON.stringify(i.name||'')})'>复制名称</button><button class="mini" onclick='LvbanCopy(${JSON.stringify(i.address||'')})'>复制地址</button><button class="mini" onclick='LvbanNav(${JSON.stringify(i.address||i.name||'')})'>导航</button><button class="mini" onclick='openItemEditor(${k})'>编辑</button><button class="mini danger" onclick='deleteItem(${k})'>删除</button></div></div>`}).join(''):`<div class="empty glass">今天还没有详细日程<br><button class="mini primary" style="margin-top:10px" onclick="openDayEditor()">＋ 添加第一条</button></div>`}</div><div class="day-footer"><span class="budget">当天预算：¥${items.reduce((s,x)=>s+Number(x.budget||0),0)}</span><button class="mini primary" onclick="openDayEditor()">＋ 添加</button></div>`;box.innerHTML=scheme+planTabs+cityBlock+dateBlock+detail;
    box.querySelectorAll('[data-city]').forEach(b=>b.onclick=()=>{window.__lvTripCity=b.dataset.city;const idx=(p.days||[]).map((x,i)=>i).find(i=>map[i]===window.__lvTripCity);if(idx!=null)window.activeDay=idx;render()});box.querySelectorAll('[data-day]').forEach(b=>b.onclick=()=>{window.activeDay=Number(b.dataset.day);render()});box.querySelectorAll('[data-plan]').forEach(b=>b.onclick=()=>{window.activePlan=b.dataset.plan;window.activeDay=0;window.__lvTripCity=null;render()});
  }
  function patchCreate(){if(window.__lvTripCreateWrapped)return true;if(typeof window.createTripFromForm!=='function')return false;const base=window.createTripFromForm;window.createTripFromForm=function(){const alternate=!!document.getElementById('ntAlternate')?.checked;const before=new Set((window.db?.trips||[]).map(x=>x.id));const result=base.apply(this,arguments);setTimeout(()=>{const list=window.db?.trips||[],t=list.find(x=>!before.has(x.id));if(!t)return;t.hasAlternateRoute=alternate;if(!alternate&&Array.isArray(t.plans)&&t.plans.length>1)t.plans=t.plans.slice(0,1);window.save?.();window.renderTrips?.()},120);return result};window.__lvTripCreateWrapped=true;return true}
  function install(){style();patchCreate();if(window.__lvTripLayoutInstalled)return;if(typeof window.renderTripDetail!=='function')return setTimeout(install,100);window.__lvTripLayoutInstalled=true;window.renderTripDetail=render;render()}
  function watch(){install();new MutationObserver(()=>setTimeout(install,30)).observe(document.body,{subtree:true,childList:true})}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',watch);else watch();
})();
